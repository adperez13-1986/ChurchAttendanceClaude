## 1. Domain Model

- [x] 1.1 Add `Attendee` case to `Category` DU in Domain.fs (after `Member`)
- [x] 1.2 Add `Attendee` to `categoryLabel` match
- [x] 1.3 Add `"Attendee"` to `parseCategory` match
- [x] 1.4 Update `allCategories` list order to Member, Attendee, UnderMonitoring, Visitor

## 2. Templates

- [x] 2.1 Add `Attendee` case to category match in `categoryOptions` (Templates.fs)

## 3. Verification

- [x] 3.1 Build the project and confirm no compiler warnings about incomplete pattern matches
- [x] 3.2 Run the app and verify Attendee appears in the add/edit member dropdown
